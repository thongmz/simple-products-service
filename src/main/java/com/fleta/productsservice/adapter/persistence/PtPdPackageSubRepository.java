package com.fleta.productsservice.adapter.persistence;

import com.fleta.productsservice.domain.model.PtPdPackageSub;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PtPdPackageSubRepository extends JpaRepository<PtPdPackageSub, String> {
}
